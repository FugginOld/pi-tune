# The target is a Raspberry Pi. A shell script checked out with CRLF fails at
# the shebang with "bad interpreter: /usr/bin/env bash^M", so LF is not a
# preference here — it is a correctness requirement. Pin it in the repo rather
# than relying on every clone's core.autocrlf.
* text=auto eol=lf
